<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!--
      A warning the user cannot see is no warning at all. Once they leave the app the
      full-screen overlay is behind whatever they opened, so the nudge has to be a buzz and a
      heads-up notification instead. VIBRATE is granted at install; nothing to ask for.
    -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!--
      Reminders are only useful if they are punctual, so exact alarms are required.
      USE_EXACT_ALARM is granted at install for apps whose core function is user-set timing;
      SCHEDULE_EXACT_ALARM is the requestable fallback on Android 12.
    -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!--
      Calls and notifications from other apps are recorded during a session so the user can be
      told which app kept interrupting them. READ_PHONE_STATE is a normal runtime permission;
      notification access is a special access that cannot be requested with a dialog, so the
      app asks for it with a card that opens the right Settings screen and works without it.
    -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <!-- The app degrades gracefully without any of these, so none is required. -->
    <uses-feature android:name="android.hardware.sensor.light" android:required="false" />
    <uses-feature android:name="android.hardware.sensor.accelerometer" android:required="false" />
    <uses-feature android:name="android.hardware.microphone" android:required="false" />

    <application
        android:name=".FocusGuardApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FocusGuard">

        <activity
            android:name=".FocusGuardActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.FocusGuard"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".FocusSessionService"
            android:exported="false"
            android:foregroundServiceType="specialUse|microphone">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Times a user-started focus session and watches the environment for conditions that break concentration. The session must keep running with the screen off, so it cannot be a short service." />
        </service>

        <!--
          Bound by the system, not by the app, so it runs on Android's schedule rather than
          only during a session — InterruptionRecorder decides what actually counts.
        -->
        <service
            android:name=".interruptions.FocusGuardNotificationListener"
            android:exported="false"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <receiver
            android:name=".alarms.SessionAlarmReceiver"
            android:exported="false" />

        <receiver
            android:name=".alarms.BootCompletedReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
